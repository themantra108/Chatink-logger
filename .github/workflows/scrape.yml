name: Chartink CSV Logger

on:
  schedule:
    - cron: '*/5 * * * *'  # Run every 5 minutes
  workflow_dispatch:      # Manual trigger button

# CRITICAL: Grant permission to write changes back to the repo
permissions:
  contents: write

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: '1'

      # Caching speeds up the run from 5 mins -> 30 seconds
      - name: Cache Julia Artifacts
        uses: julia-actions/cache@v1

      - name: Run Scraper
        run: julia scraper.jl

      - name: Commit and Push Data
        run: |
          git config user.name "Chartink Bot"
          git config user.email "actions@github.com"
          git add *.csv
          # The "|| echo" prevents the workflow from failing if there's no new data
          git commit -m "Auto-update scan data [skip ci]" || echo "No changes to commit"
          git push
