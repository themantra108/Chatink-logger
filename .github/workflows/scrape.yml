      # ... (Previous steps remain the same) ...

      - name: Commit and Push 💾
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 1. Pull FIRST (Safe because we haven't touched chartink_history.csv yet)
          git pull --rebase
          
          # 2. Append the temp data to the main history file
          # (If history file doesn't exist, this creates it)
          if [ -f new_chunk.csv ]; then
            cat new_chunk.csv >> chartink_history.csv
            rm new_chunk.csv
            echo "✅ Data appended successfully."
          else
            echo "⚠️ No new data chunk found."
            exit 0
          fi
          
          # 3. Stage and Commit
          git add chartink_history.csv
          timestamp=$(date -u)
          git commit -m "📈 Data: ${timestamp}" || echo "No changes to commit"
          
          # 4. Push
          git push
